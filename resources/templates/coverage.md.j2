### :globe_with_meridians: Coverage report

Name | Metrics % (`covered/total`) | Diff
 --- | --- | ---
{% for key, value in data.items() -%}
{%- set status = ":grey_exclamation:" -%}
{%- set diff = "" -%}
{%- if value.previousRatio -%}
  {%- if value.previousRatio > value.ratio -%}
      {%- set status = ":-1:" -%}
  {% else %}
      {%- set status = ":+1:" -%}
  {%- endif -%}
{% else %}
  {%- if value.ratio|int == 100 -%}
    {%- set status = ":green_heart:" -%}
  {%- endif -%}
{%- endif -%}
{{ key }} | {{ value.ratio|float|round(3) }}% (`{{ value.numerator }}/{{ value.denominator }}`) | {{ status }} {% if value.previousRatio %} `{{ ( value.ratio|float|round(3) - value.previousRatio|float|round(3) ) }}` {% endif %}
{% endfor %}
