# Declare your Buildkite pipelines below
---
# yaml-language-server: $schema=https://gist.githubusercontent.com/elasticmachine/988b80dae436cafea07d9a4a460a011d/raw/rre.schema.json
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: buildkite-pipeline-apm-pipeline-library
  description: Buildkite Pipeline for apm-pipeline-library
  links:
    - title: Pipeline
      url: https://buildkite.com/elastic/apm-pipeline-library

spec:
  type: buildkite-pipeline
  owner: group:observablt-robots
  system: buildkite
  implementation:
    apiVersion: buildkite.elastic.dev/v1
    kind: Pipeline
    metadata:
      name: apm-pipeline-library
      description: 
    spec:
      repository: elastic/apm-pipeline-library
      pipeline_file: ".buildkite/pipeline.yml"
      teams:
        observablt-robots:
          access_level: MANAGE_BUILD_AND_READ
        everyone:
          access_level: READ_ONLY
